<script setup>
import { Head, Link } from '@inertiajs/vue3';
import { computed } from 'vue';
import AtalaFoto from '@/Components/AtalaFoto.vue'; // Mengaktifkan komponen foto kamu

const props = defineProps({
    name: String,
    role: String,
    projects: Array
});

// Counter dinamis berdasarkan jumlah project yang diupload
const completedProjects = computed(() => {
    return props.projects ? props.projects.length : 0;
});
</script>

<template>
    <Head title="Portfolio" />

    <div class="min-h-screen bg-[#0a0a0a] text-white selection:bg-blue-500/30">
        <nav class="fixed top-0 w-full z-50 flex justify-between items-center px-10 py-8 backdrop-blur-md bg-black/40 border-b border-white/5">
            <div class="text-3xl font-black tracking-tighter italic">AA.</div> 
            <div class="hidden md:flex gap-12 text-[10px] font-bold uppercase tracking-[0.3em] text-gray-500">
                <a href="#about" class="hover:text-white transition">About Me</a>
                <a href="#portfolio" class="hover:text-white transition">Portfolio</a>
                <a href="#" class="hover:text-white transition">Services</a>
                <a href="#" class="hover:text-white transition">Blog</a>
            </div>
            <a href="mailto:aditia.anwar08@gmail.com" class="border border-white/20 px-8 py-3 rounded-full text-[10px] font-bold uppercase tracking-widest hover:bg-white hover:text-black transition duration-500">
                Contact Me
            </a>
        </nav>

        <section id="about" class="relative min-h-screen flex items-center pt-20 overflow-hidden">
            <div class="container mx-auto px-10 grid grid-cols-1 lg:grid-cols-12 items-center gap-12">
                
                <div class="z-10 lg:col-span-7 pt-20">
                    <div class="flex gap-16 mb-12">
                        <div>
                            <span class="block text-6xl font-bold text-white tracking-tighter">+{{ completedProjects }}</span>
                            <span class="text-[10px] uppercase tracking-[0.2em] text-gray-600 font-bold">Project completed</span>
                        </div>
                        <div>
                            <span class="block text-6xl font-bold text-white tracking-tighter">+3</span>
                            <span class="text-[10px] uppercase tracking-[0.2em] text-gray-600 font-bold">Years Experience</span>
                        </div>
                    </div>
                    
                    <h1 class="text-[10rem] lg:text-[14rem] font-bold leading-[0.8] tracking-tighter mb-8">
                        Hello
                    </h1>
                    <p class="text-2xl lg:text-3xl text-gray-400 max-w-xl leading-snug font-light">
                        — It's <span class="text-white font-semibold">{{ name }}</span>, a dedicated <span class="text-white font-semibold">{{ role }}</span> focused on high-quality software delivery.
                    </p>

                    <div class="mt-24">
                        <a href="#portfolio" class="inline-flex items-center gap-4 text-[10px] font-bold uppercase tracking-[0.4em] text-gray-600 hover:text-white transition group">
                            Scroll down 
                            <span class="text-lg group-hover:translate-y-2 transition-transform duration-500">↓</span>
                        </a>
                    </div>
                </div>

                <div class="lg:col-span-5 flex justify-center lg:justify-end pt-10">
                    <div class="w-full max-w-[550px] aspect-[3/4] bg-neutral-900 rounded-[2rem] overflow-hidden border border-white/5 shadow-2xl relative group">
                        <div class="w-full h-full grayscale hover:grayscale-0 transition duration-1000 ease-in-out transform hover:scale-105">
                             <AtalaFoto />
                        </div>
                        <div class="absolute inset-0 bg-gradient-to-t from-[#0a0a0a] via-transparent to-transparent opacity-40"></div>
                    </div>
                </div>
            </div>

            <div class="absolute left-10 bottom-10 hidden lg:block">
                <span class="rotate-[-90deg] block origin-left text-[10px] font-bold uppercase tracking-[0.6em] text-gray-700">
                    2026 / Niloure
                </span>
            </div>
        </section>

        <section id="portfolio" class="py-32 bg-[#0d0d0d] border-t border-white/5 rounded-t-[4rem]">
            <div class="container mx-auto px-10">
                <div class="flex flex-col md:flex-row justify-between items-end mb-24 gap-10">
                    <div>
                        <h2 class="text-7xl lg:text-9xl font-bold tracking-tighter mb-4">My</h2>
                        <h2 class="text-7xl lg:text-9xl font-bold tracking-tighter text-neutral-800 italic">Project</h2>
                    </div>
                    <p class="text-gray-500 max-w-sm mb-6 text-lg leading-relaxed">
                        A collection of automation frameworks and tools I've built to ensure software excellence.
                    </p>
                </div>

                <div class="grid grid-cols-1 md:grid-cols-2 gap-x-16 gap-y-32">
                    <div v-for="project in projects" :key="project.id" class="group">
                        <div class="aspect-video bg-neutral-900 rounded-[2.5rem] overflow-hidden mb-10 relative border border-white/5">
                            <div class="absolute inset-0 flex items-center justify-center bg-black text-white text-6xl font-black italic opacity-20 group-hover:opacity-100 transition-all duration-700 group-hover:scale-110">
                                {{ project.title.substring(0, 2) }}
                            </div>
                        </div>
                        
                        <div class="flex justify-between items-start px-2">
                            <div>
                                <h3 class="text-3xl font-bold mb-4 group-hover:text-blue-500 transition-colors duration-300">{{ project.title }}</h3>
                                <div class="flex flex-wrap gap-3">
                                    <span v-for="tech in project.tech_stack.split(',')" :key="tech" class="text-[9px] font-bold uppercase tracking-widest bg-white/5 border border-white/10 px-4 py-2 rounded-full text-gray-400 group-hover:border-white/20 transition">
                                        {{ tech.trim() }}
                                    </span>
                                </div>
                            </div>
                            <a v-if="project.link" :href="project.link" target="_blank" class="w-16 h-16 border border-white/10 rounded-full flex items-center justify-center text-2xl hover:bg-white hover:text-black hover:scale-110 transition-all duration-500">
                                ↗
                            </a>
                        </div>
                    </div>
                </div>
            </div>
        </section>

        <footer class="py-20 text-center border-t border-white/5">
            <p class="text-gray-600 text-[10px] uppercase tracking-[0.5em]">© 2026 Atala Aditia Anwar — Built with ❤️ and Laravel</p>
        </footer>
    </div>
</template>

<style>
@import url('https://fonts.googleapis.com/css2?family=Inter:wght@300;400;600;800;900&display=swap');

html {
    scroll-behavior: smooth;
    background-color: #0a0a0a;
}

body {
    font-family: 'Inter', sans-serif;
}

/* Menghilangkan scrollbar tapi fungsi scroll tetap ada */
::-webkit-scrollbar {
    width: 0px;
}
</style>